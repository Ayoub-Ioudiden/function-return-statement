const check = function (number) {
    const sum = number;
    if (sum > 100) {
        return "True";
    }
    return "False";
};

const result = check(99);
console.log(result);


const enterOrNoEnter = function (age, guests) {
    const sum = age + guests;
    if (sum >= 18) {
        return "come in"
    }
    else if (sum > 100) {
        return "it's too busy now, come back later";
    }
    else


}











